docker {
    name project.name + ":" + version
    dockerfile file(rootProject.projectDir.toPath().resolve('Dockerfile'))
    files tasks.bootJar.outputs.files
    buildArgs(['JAR_FILE': tasks.bootJar.outputs.files.singleFile.name])
}